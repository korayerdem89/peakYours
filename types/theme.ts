export interface ThemeColors {
  primary: {
    light: string;
    dark: string;
  };
  secondary: {
    light: string;
    dark: string;
  };
  accent: {
    light: string;
    dark: string;
  };
  background: {
    light: string;
    dark: string;
  };
  surface: {
    light: string;
    dark: string;
  };
  text: {
    light: string;
    dark: string;
    'light-secondary': string;
    'dark-secondary': string;
  };
  error: {
    light: string;
    dark: string;
  };
}
